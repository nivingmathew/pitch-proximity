services:
  postgres:
    image: postgres:16-alpine
    container_name: pitch-proximity-db
    environment:
      POSTGRES_DB: pitch_proximity
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    restart: always